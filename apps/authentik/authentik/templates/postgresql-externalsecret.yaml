apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: postgresql
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical
  refreshInterval: "0"
  data:
    - secretKey: username
      remoteRef:
        key: POSTGRESQL_USER
    - secretKey: password
      remoteRef:
        key: POSTGRESQL_PW
